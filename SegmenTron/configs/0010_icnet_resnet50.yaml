DATASET:
    NAME: "datasetAC"
    MEAN: [0.485, 0.456, 0.406]
    STD: [0.229, 0.224, 0.225]
TRAIN:
    EPOCHS: 60
    BATCH_SIZE: 8
    CROP_SIZE: 512
    BASE_SIZE: 512
    SUPERVISE_SIZE: 512
    BACKBONE_PRETRAINED_PATH: "pretrain/resnet50-19c8e357.pth"
TEST:
    BATCH_SIZE: 4
    SCALES: [0.75, 1.0, 1.25]

SOLVER:
    LR: 0.04
    OPTIMIZER: "SGD"
#    USE_FP16: True
    AUX: True

MODEL:
    MODEL_NAME: "ICNet"
    BACKBONE: "ibn_resnet50"
    OUTPUT_STRIDE: 8

